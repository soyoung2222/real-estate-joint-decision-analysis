> **관련 활동** · PRD 성공지표 설계 보강 (오프닝 가이드 §10 Day5 "KPI·검증방법" 예정 항목을 미리 보강)
> **기준 문서** · 과목02 [D2] 제품 전략·요구사항 정의(PRD)와 지표 기반 의사결정 §5~6, GitHub `soyoung2222/real-estate-joint-decision-analysis` `assets/reverse-planning.html` §15
> **상태** · 기존 KPI 분석 + D2 기준 보강안

# KPI 설계 보강 — D2 기준 가드레일 지표 신설

## 1. 이미 D2 기준을 충족하는 부분 (그대로 유지)

| D2 원칙 | 원문 근거 | GitHub §15에서 이미 하고 있는 것 |
|---|---|---|
| 허영지표 배제, 행동지표 사용 | "가입자 수 같은 허영지표는 오르기만 하고 의사결정을 못 바꾼다" | North Star가 "발송 세션 중 7일 내 방문후보 2개 확정 세션"으로 **행동 완주**를 잼. "가입자 수·다운로드 수" 류가 전혀 없음 |
| NSM은 분모·기간을 명확히 | — | "기간이 없으면 언젠가는 합의함이 되어 측정 불가", "분모가 시작한 사용자면 A 혼자 담다 만 것까지 들어간다"를 이미 스스로 지적하고 고침 |
| 절대 기준선 대신 상대 비교 | RICE 절 "점수 자체가 아니라 가정을 공개" 정신과 동일 | "비교군 수치가 비공개라 절대 기준선을 만들 수 없다 → 집단 간 비교로 판정"(반증기준 표) |
| 측정 가능성을 정직하게 구분 | "확신 없는 수치는 확인 필요로 표시" | 프로토타입가능/출시후만/내부데이터불가 3단 구분 — Conversion 항목 전체를 "측정하겠다고 하면 안 된다"고 명시 |

**이 네 가지는 D2가 요구하는 수준을 이미 넘어섭니다.** H1을 세 조각(a/b/c)으로 쪼갠 것도 "지표 트리" 개념(§5 심화)을 독자적으로 재발명한 것입니다.

## 2. 완전히 빠진 것 — 가드레일 지표

D2 §5의 핵심 문장: **"NSM 하나에 입력 지표 3~4개, 각 입력 지표에 가드레일(악화되면 안 되는 지표)을 짝지으면 '이 숫자를 올리려다 저 숫자를 망가뜨리는' 사고를 막는 지표 트리가 된다."**

GitHub §15에는 North Star와 깔때기(입력지표)만 있고 **가드레일 지표가 하나도 없습니다.** 이건 빈칸이 아니라 실제 위험입니다 — North Star("방문후보 2개 확정 비율")를 올리기 위해 UX를 밀어붙이면 다음이 망가질 수 있는데, 그걸 감시할 지표가 지금 없습니다.

### 추천 가드레일 지표 3개 (근거 포함)

| 가드레일 지표 | 무엇을 막나 | 근거 |
|---|---|---|
| **방문 후 '제외' 비율 (헛방문율)** | NSM을 억지로 밀어붙여 "확정은 됐지만 실제로는 안 맞는" 가짜 합의가 생기는 것 | 이미 `00-proposal.md` §6에 "헛걸음 지표"로 존재하지만 **후행지표로만 분류돼 있고 NSM의 가드레일로 연결돼 있지 않음.** D2: "탐지율과 전환율은 하나의 트레이드오프 — 하나만 보면 반드시 다른 하나가 망가진다"를 그대로 대입하면, 확정률↑과 헛방문율↑이 동시에 뛰면 그건 성과가 아니라 사고임 |
| **충돌 화면 노출 후 이탈률** | 조건 충돌·양보 지점을 보여주려다 오히려 갈등을 키워 사용자가 이탈하는 것 | `decisions/0001`이 이미 "충돌 화면이 다툼을 키운다"를 리스크로 인지하고 "승패 표현 배제"로 대응했지만, 이게 **지표로 감시되지는 않음.** 대응 설계가 있어도 측정이 없으면 실패를 모름(D2: "화면 동작 너머의 실무 예외" — 7절 3질문의 리스크 축과 동일한 논리) |
| **조건 재입력·재설정 비율** | 입력 마찰을 줄이려고(H3 완화) 항목을 너무 단순화한 부작용으로, 나중에 조건을 자꾸 고쳐야 하는 것 | H3 완화 설계("한 번에 안 묻고 하나씩") 자체는 맞지만, 너무 단순화하면 초기 조건이 실제와 안 맞아 재입력이 급증할 수 있음 — 이 트레이드오프를 감시할 지표가 없었음 |

## 3. KPI 설계표 — D2 워크시트 ② 양식으로 재구성

D2 워크시트 ②는 `계열 | KPI | 현재값 | 목표값 | 측정방법 | 가드레일 지표 | 가정/검증` 7칸을 요구합니다. 기존 §15의 깔때기 표를 이 양식에 맞춰 재구성하면:

| 계열 | KPI | 현재값 | 목표값 | 측정 방법 | 가드레일 지표 | 가정/검증 |
|---|---|---|---|---|---|---|
| 초대전환 | 링크 열람률 (H1-a) | 측정 필요 | 측정 필요 | 이벤트 로그(발송·열람) | — | (가정) A의 권유 문구가 통함 → (확인) 열람률 50% 미만이면 진입 UX 재설계 |
| 초대전환 | B 조건입력 완료율 ★ (H1-b, 급소) | 측정 필요 | 측정 필요 | 이벤트 로그 | 조건입력 중도이탈 지점 | (가정) 4번 입력(`decisions/0003`)이면 완료율↑ → (확인) 이탈 지점이 특정 항목에 몰리면 그 항목만 선택형으로 전환 |
| 입력완성 | A 1단계(2개) 완료율 (H3) | 측정 필요 | 측정 필요 | 이벤트 로그 | — | H3 |
| 입력완성 | A 확장(3~4개) 완료율 (H3) | 측정 필요 | 측정 필요 | 이벤트 로그 | **조건 재입력률** | 너무 단순화하면 재입력 급증 — 위 §2 참고 |
| 합의품질 | 조건 완화 실행률 (H2-b) | 측정 필요 | 측정 필요 | 이벤트 로그 | **충돌화면 이탈률** | 완화를 유도하려다 갈등을 키우면 안 됨 |
| 합의품질(NSM) | 방문후보 2개 확정 비율 | 측정 필요 | 측정 필요 | 이벤트 로그(발송~확정, 7일 창) | **방문 후 제외율(헛방문)** | "확정됐지만 실제로는 안 맞는" 가짜 완주 감시 |
| 참여지속 (출시 후) | 로그인 통과율 (H1-c) | 출시 후 | 출시 후 | 로그인 이벤트 | — | 반증기준표(§15 원문)의 4분면 판정에 사용 |

### 트레이드오프 선언 (D2 §5 형식)

> **올리려는 지표**: 방문후보 2개 확정 비율(North Star)
> **함께 감시할 가드레일**: 방문 후 제외 비율(헛방문), 충돌 화면 이탈률
> **판정 규칙**: North Star가 오르는데 두 가드레일도 같이 나빠지면, 그건 개선이 아니라 "숫자만 맞춘 가짜 확정"이다.

## 4. 참고 — 그대로 가져오면 안 되는 것

D2의 NSM 예시("주간 QR 결제 습관 사용자")는 **습관성·고빈도 지표**입니다. 우리 서비스는 집을 구하는 몇 달에 한 번뿐인 저빈도 행동이라 "습관 형성" 개념을 억지로 넣을 필요는 없습니다. 지금의 "발송→7일내 확정" 완주형 North Star가 우리 도메인에는 더 맞는 선택이며, 이 판단 자체를 Decision Log에 한 줄로 남겨두는 걸 권장합니다.

Mission Critical 4요건(정확성·안전성·신뢰성·보안성)도 핀테크 전용 개념이라 그대로 쓸 필요는 없지만, 정신만 옮기면: **정확성**(실질 주거비 추정 오차·전제 고지), **안전성**(예산·직장 등 민감정보 노출 방지), **보안성**(조건 데이터는 초대된 상대만 열람) 세 가지는 이미 우리 리스크 표(`00-proposal.md` §7)에 흩어져 있는 내용과 그대로 겹칩니다 — 필수는 아니지만 발표 때 "우리도 합격선 개념을 검토했다"는 근거로 한 줄 언급하면 좋습니다.
