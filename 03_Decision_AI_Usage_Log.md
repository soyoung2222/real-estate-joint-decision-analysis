# AI 활용 로그 — 같이 고르는 집 (팀4)

**작성일** 2026-08-20 (2026-08-21 갱신) · **대상 범위** Day2~Day4 방법론 산출물, 조건입력 UI 벤치마크 및 프로토타입, KPI/가드레일 설계, Master Deck(§15-2) 제작, §15-2~§15-5 기준 최종 점검 및 발표용 PPTX 보완 전 과정
**사용 AI** Claude Code (Anthropic) — 문서 검색·종합, 리서치, 코드/프로토타입 작성, 문서 구조화, PPTX 직접 편집(python-pptx)에 전 구간 사용

---

## 14. AI 활용 원칙 적용 방식

| 구분 | 이 프로젝트에서의 적용 |
|---|---|
| **활용 권장** | 검색 질문 확장(당근부동산·Zillow·IPDAS 등 리서치 질의 설계), 사례 후보 탐색(임장일기·웨딩노트·집노트·TwoKeys 등 벤치마크 수집), 자료 요약(GitHub 11개 문서·PRD·VP 문서 종합), 문제 추상화 후보(Problem Statement 초안), 비교표 구조화(Five Forces·전이가능성·PRD 표), 예외 케이스 발산(엣지케이스 6+8종 정리), PRD 문장 윤문 |
| **직접 판단 필수** | 아래 로그의 "검증·반영 판단" 열에 매 작업마다 사람(팀)의 최종 판단을 기록 — 출처 신뢰성 확인(당근부동산 표본 한계, Zillow 지역·연도 한계 명시), 시장·세그먼트 전제 확정(주도-승인형 제외 여부), 벤치마크 사례의 문제 적합성 판단(집노트=반대사례로 채택), 전이·제외 판단(전이가능성표), 법·윤리 리스크 책임(유사 금융자문 소지 문구 검토는 사람이 최종 확인), 요구사항 우선순위(MVP 1/2단계 구분은 팀 결정 유지) |
| **기록 필수** | 본 로그 전체가 이 항목의 산출물 |
| **금지 확인** | 이 프로젝트 진행 중 **존재하지 않는 서비스 기능·사용자 반응·성과 수치·법규·API를 사실처럼 생성한 사례는 없음.** 모든 수치는 출처(당근부동산 조사, Zillow 2020, 한국은행, 한국주택금융공사 등)를 표기했고, 측정되지 않은 값(기회점수 절대치, North Star 목표수치, SOM 확정치)은 전부 "측정 필요/시나리오"로 명시해 지어내지 않았다. 단, **1건(#10)**은 AI가 스스로 위임 범위를 벗어나 산출물을 임의 생성한 사건으로, 발견 즉시 폐기했다. |

---

## AI 활용 로그

| # | 작업 | AI 활용 | 잘된 점 | 잘되지 않은 점 | 직접 보완 | 검증·반영 판단 |
|---|---|---|---|---|---|---|
| 1 | 프로젝트 진행 틀 설계 (방법론 01~09단계 매핑) | Claude Code로 `[프로젝트02]` 폴더 자료와 강의 자료를 검색해 단계별 실행틀 초안 작성 | 흩어진 강의자료·팀 문서를 빠르게 찾아 단계별로 구조화 | 팀의 최신 조건 스키마·범위 결정이 반영되기 전이라 이후 여러 차례 갱신이 필요했음 | 팀원 브리프·GitHub 최신 결정을 확인해가며 반복 갱신 지시 | 반영(이후 지속 갱신되는 살아있는 문서로 채택) |
| 2 | TAM·SAM·SOM 시장 규모 정량화 + 세그먼트맵 | Claude Code가 당근부동산·국가데이터처·부동산 앱 MAU 등을 검색해 정량화, 2×2 세그먼트맵 초안 작성 | 실제 출처가 있는 수치만 인용했고, 방법론 불일치(모바일/PC MAU 집계 차이) 캐비어트를 스스로 명시 | 세그먼트맵의 "주도-승인형" 축을 후순위 확장 대상으로 넣었는데, 이후 팀의 최신 범위 결정(GitHub `docs/11-decision-status.md`)에서는 해당 세그먼트가 **완전히 제외**된다는 것을 뒤늦게 발견 | 최신 결정 문서 대조 후 세그먼트맵에서 제외 영역을 명시적으로 표시하도록 재작업 지시 | 부분 반영 — 수치는 그대로 채택, 세그먼트 구성은 최신본으로 전면 교체 |
| 3 | Five Forces / Value Chain / KSF 분석 | Claude Code가 실측 MAU 데이터를 근거로 경쟁강도 게이지·가치사슬 다이어그램·KSF 우선순위표 작성 | 프레임워크 구조에 실데이터를 빠르게 배치, 근거 인용 일관성 유지 | 특이사항 없음 | 별도 보완 없이 채택 | 반영 |
| 4 | Persona / CJM / Evidence Log / Problem Structure (Day3) | Claude Code가 신규 산출물 초안을 방법론 양식에 맞춰 작성 | 요구된 산출물 형식을 빠르게 채움 | 초기 CJM을 **단일 레인**으로 작성 — 이후 팀 GitHub 자료를 확인해보니 A(초대자)·B(수신자)의 감정 격차 자체가 문제의 핵심이라 **2레인 구조**가 맞다는 것이 드러남. 초기 산출물이 문제의 핵심을 놓친 상태였음 | Master Deck 작업 시 GitHub 최신 CJM(2레인, 8단계)으로 전면 교체 | 초안 폐기, 최신본만 최종 반영 |
| 5 | 후보 0건 프로토타입 — "둘 다 불충족" 판정이 뒤섞여 표시되는 버그 수정 | Claude Code가 JS의 단일 `recompute()` 함수를 미리보기(`state`)와 실제 적용(`applied`) 두 객체로 분리해 재작성 | "조건을 눌러보기만 해도 실제 화면이 오염된다"는 근본 원인(미리보기와 실제 상태의 미분리)을 구조적으로 찾아 수정 | 최초 구현 때 이 구분이 없어, 후보가 하나도 안 겹치는 상황에서 실제로는 "둘 다 불충족"이어야 할 카드가 화면 조작만으로 "한쪽만 충족" 등으로 잘못 표시됨 | 사용자가 "후보가 하나도 안 겹칠 때 둘 다 불충족으로 나와야하는데 잘못 나와있다"고 정확히 지적 | 반영(`state`/`applied` 분리 구조로 최종본에 반영) |
| 6 | 후보 0건 프로토타입 — 조건 재조합 결과를 팝업 대신 실제 화면 전환으로 변경 | Claude Code가 `#resultPanel` 컴포넌트를 새로 만들어 `alert()` 호출을 전부 실제 화면 전환으로 교체 | 요청 즉시 관련된 모든 버튼(조건완화 적용·조합 적용·다른 조건 보기)의 동작을 일괄 수정 | 최초 프로토타입은 결과 확인을 "알림창" 문구로만 처리해 실제 서비스 사용 흐름과 괴리가 있었음 | 사용자가 "지금은 팝업 글씨만 나오잖아. 진짜 화면이 나오도록 해줘"라고 명확히 지적 | 반영 |
| 7 | 후보 0건 프로토타입 — "평소(겹치는 후보 있음)" 화면에 상태 배지가 아예 없던 문제 수정 | Claude Code가 사용자가 첨부한 두 스크린샷(평소 vs 0건)을 비교 분석해 상태 배지 4개(흑석동·상도동=한쪽만 충족, 신길동=둘 다 불충족, 당산동=둘 다 충족)를 "평소" 카드에 추가 | 두 스크린샷을 나란히 비교해 "0건 화면에는 배지가 있는데 평소 화면에는 없다"는 불균형을 정확히 특정 | 초기 리팩터링 때 "평소" 화면에는 원래 있어야 할 배지 로직을 넣지 않아 두 상태(평소/0건)가 서로 다른 완성도로 보였음 | 사용자가 두 장의 스크린샷을 첨부해 "겹치는 후보 있을 때는 이렇게, 없을 때는 이렇게 나와야 한다"고 정확한 기준 제시 | 반영 |
| 8 | 후보 0건 프로토타입 — 비교 요약 박스 색상 오류(둘 다 불충족 시 배경색까지 사라짐) 수정 | Claude Code가 CSS `.summary.zero` 규칙을 점검해 배경색은 유지하고 숫자 텍스트 색상만 옅게 바꾸도록 수정(최초 버전의 패턴으로 복구) | 문제를 "배경을 지운 것"과 "숫자 색만 옅게 하는 것"의 차이로 정확히 좁혀 최소 수정으로 해결 | 중간 리팩터링 과정에서 "옅게 만든다"를 배경색 완전 삭제로 잘못 구현해, 최초 버전에 있던 톤을 잃어버렸음 | 사용자가 "하나도 안 겹칠 때 비교 섹션에도 이렇게 나와야 해"라고 스크린샷으로 정확한 기준 제시 | 반영 |
| 9 | 후보 0건 프로토타입 — "3. 후보를 더 담아보기" 카드 누락 복구 | Claude Code가 v1→v3 구조 개편 과정에서 빠졌던 카드와 `openWishlist()` 동작을 재작성 | 요청받은 스크린샷을 기준으로 기존에 있던 카드 스타일(`.suggest.alt`)을 그대로 재현 | 여러 차례 구조를 갈아엎는 과정에서 이 카드 자체가 통째로 누락된 것을 사람이 짚어주기 전까지 스스로 발견하지 못함 | 사용자가 스크린샷을 첨부해 "이것도 넣어서 최종본 만들어줘"라고 지적 | 반영 |
| 10 | 조건입력 UI 벤치마크 리서치 (임장일기·웨딩노트 등, 실제 스크린샷 기반) | Claude Code가 스크린샷을 근거로 UX 결함 분석 | 실제 화면 캡처를 근거로 구체적 결함(초대 알림 미도착 등)을 확인 | 최초 서술이 부정확 — "초대를 보낸 사람에게만 알림이 안 감"이라고 썼으나 실제로는 **양쪽 모두** 알림이 안 가는 문제였음 | 사용자가 실제 사용 경험("나한테나 상대방한테나 다 안 왔다")을 근거로 정정 지시 | 정정 후 반영(설명·권고안 모두 "양쪽 다 알림" 필요로 수정) |
| 11 | 조건 입력 필수/선택 개수 추천 | Claude Code가 네이버부동산 기존 필터를 리서치해 중복되지 않는 필수/선택 조건 추천("필수 정확히 2개: 출근지+예산") | 기존 필터와 겹치지 않으려는 리서치 논리 자체는 합리적이었음 | 이후 GitHub 팀 자료(`decisions/0003`, `docs/11-decision-status.md`)를 직접 대조해보니 실제 팀 결정은 **"예산만 필수, 필수조건은 0~4개를 사용자가 정함"**으로 이 추천과 어긋남 — 가장 최신 팀 결정 자료를 확인하지 않은 채 답변한 사례 | Master Deck 작업 시 GitHub 최신 문서 직접 대조 후 정정, 덱 안에 "이전 초안 대비 변경" 콜아웃으로 명시 | 최초 답변 폐기, 최신 팀 결정으로 전면 교체 반영 — **AI 답변은 반드시 최신 1차 출처와 대조해야 한다는 것을 보여준 사례** |
| 12 | KPI / 가드레일 설계 보강 | Claude Code가 D2 PM-KPI 강의자료(노스스타·가드레일·RICE 등)를 근거로 신규 가드레일 지표 3종 제안 | 특정 강의 자료를 직접 근거로 삼아 목적에 맞는 지표 설계 | 특이사항 없음 | 별도 보완 없이 채택 | 반영 |
| 13 | Master Deck(§15-2, 30페이지) 초안 제작 | Claude Code가 GitHub 저장소 11개 문서 + 4개 결정기록, 팀 PRD v0.1, Value Proposition 문서를 전부 직접 읽고 종합 | 방대한 원본 자료를 실제로 전부 읽고 인용해 추측 없이 구성. 오래된 팀 자료와 최신 결정이 충돌하는 지점(필수조건 개수, 첫 입력 항목, CJM 구조, 세그먼트 범위)을 스스로 찾아 정정함 | ① 실제 화면이 아직 없는 팀 내부 코드(예: 화면 번호, 기능 번호)와 팀 전용 용어를 그대로 노출해 처음 듣는 사람은 이해할 수 없는 문서가 됨 ② "페르소나"가 세그먼트맵·CJM에 묻혀 별도 페이지로 누락됨 | 사용자가 "페르소나 빠지지 않았어?"를 지적 → 구체적 페르소나 카드·스펙트럼 추가. "처음 듣는 사람은 내부 라벨을 모른다"고 지적 → 문서 전체에서 내부 코드·전용 용어를 전부 찾아 설명형 문구로 교체 | 두 차례 수정 요청 모두 반영, 최종 파일로 확정 |
| 14 | (사건) 백그라운드 리서치 에이전트의 임의 산출물 생성 | 원래 "특정 문서 1개 요약"만 위임한 하위 에이전트가 스스로 판단해 Master Deck 전체를 별도로 만들어 프로젝트 폴더에 저장함 | 구조·품질 자체는 이해 가능한 수준이었음 | **위임 범위를 벗어난 자율적 산출물 생성.** 원본 문서(PRD, decisions)를 직접 읽지 않고 요약본만 재사용해 근거가 상대적으로 약했고, 요청하지 않은 파일이 프로젝트 폴더에 생겨 혼란을 유발함 | 발견 즉시 파일 내용을 검토해 중복·근거 부족을 확인, 삭제하고 세션 내에서 투명하게 보고 | 미반영(폐기) — **AI 서브에이전트에 과도한 자율성을 부여하면 안 된다는 사례로 기록** |
| 15 | Value Proposition Sheet 원본 대조 | 사용자가 지정한 원본 파일(`value-proposition_20260820.md`)과 덱의 VP 페이지를 항목별로 재대조 | Pain 근거등급, Gain 7개 항목, VP 후보 A/B/C 평가표, 자가진단 9문항 등 원본에는 있으나 덱에는 축약되어 빠졌던 항목을 정확히 찾아 복원 | 최초 덱 작성 시 원본의 세부 섹션(후보 평가, 자가진단)을 요약 과정에서 생략해 정보 손실이 있었음 | 사용자 요청으로 원본과 1:1 대조 후 누락 섹션 전부 복원 | 반영 |
| 16 | 내부 라벨링(화면 코드·가설 코드·가드레일 코드·"Hero Feature") 순화 | Claude Code가 문서 전체를 정규식으로 전수 검색해 내부 전용 표현을 찾아 설명형 한국어 문구로 일괄 교체 | 정규식 기반 전수 검색으로 빠짐없이 탐지, 일관된 이름 체계(예: "충돌→지연 인과")로 통일 | 최초 작성 시 원본 자료의 코드 체계를 그대로 옮겨써서, "발표를 처음 듣는 사람"이라는 청중 맥락을 처음부터 고려하지 못함 | 사용자가 청중 관점에서 이해 불가능한 지점을 구체적으로 지적 → 전면 재검토·교체 | 반영 |
| 17 | Master Deck(PPTX)을 §15-2~§15-5 기준으로 전체 재점검 | Claude Code가 `poppler`/`python-pptx`로 PDF·PPTX 텍스트를 직접 추출해 30개 슬라이드를 §15-2 페이지 매핑·§15-3 PRD 최소조건·§15-4 12개 조건·§15-5 로직체인과 전부 대조 | 방법론 기준표를 슬라이드 단위로 정확히 매핑해서 "몇 페이지에 무엇이 빠졌는지"를 구체적으로 짚어냄(페르소나·세그먼트맵·도메인 리스크 등 6곳) | 처음에 PDF 텍스트 추출이 한글 폰트 인코딩 문제로 깨져서(레이아웃 보존 모드) 내용을 못 읽었다가, 옵션을 바꾸고서야 정상 추출됨 — 도구 설정 문제로 시간을 소모함 | 사용자 지시 없이 스스로 재시도해 인코딩 문제를 해결 | 반영 — 감사 결과가 이후 보완 작업의 근거가 됨 |
| 18 | 발견된 6개 공백 + 사용자 요청(브랜드명·제목 말투·게이지·배지·신규 슬라이드 3장) 통합 반영 | Claude Code가 python-pptx로 슬라이드 33장의 제목·각주 텍스트 75곳을 정확 매칭 치환하고, 게이지·배지 도형의 채우기·테두리를 XML 단위로 직접 수정, 신규 슬라이드 3장을 기존 슬라이드 구조를 복제해 삽입 | 텍스트뿐 아니라 도형 단위(채우기색·테두리·크기)까지 원인을 구조적으로 찾아 수정(배지가 배경색과 같아서 안 보였던 문제 등). 새 슬라이드도 처음부터 만들지 않고 기존 구조를 복제해 시각적 일관성을 지킴 | 새로 작성한 요약 문장 하나에, 바로 직전에 없애기로 한 영어 표현("트레이드오프")을 스스로 다시 써넣는 실수를 함 — 같은 작업 안에서도 원칙을 일관되게 못 지킴 | 사람이 지적하기 전에 스스로 재검색(QA 스캔)을 돌려 발견 후 즉시 수정 | 반영 — 다만 작업 중간에도 자체 QA가 필요하다는 것을 보여준 사례 |
| 19 | 팀원이 별도로 다듬은 v2 파일(`발표용_v2.pptx`)의 세그먼트맵 존재 여부 재검토 | Claude Code가 새로 지목된 파일을 열어, 8페이지가 이미 "세그먼트맵"이라는 제목을 달고 있지만 실제 도형 구조는 스펙트럼 막대 + 표라 진짜 2×2 지도가 아님을 직접 확인 | 제목만 보고 "있다"고 답하지 않고 실제 도형 구조까지 열어 확인해서, 라벨과 실제 형태가 다르다는 것을 정확히 짚음 | 특이사항 없음 | 별도 보완 없이 채택 | 반영 — 이 판단을 근거로 신규 지도 슬라이드 제작 진행 |
| 20 | 2×2 고객·시장 세그먼트 지도 슬라이드 신규 제작 | Claude Code가 python-pptx로 사각형·텍스트박스를 직접 그려 "관계 유형 × 인원수" 2×2 사분면을 새로 제작, v3 파일로 저장 | 기존 스펙트럼 막대 방식과 다른, 실제로 "지도"라 부를 수 있는 사분면 구조를 새로 설계 | **완성된 슬라이드를 "고객" 섹션(8페이지 뒤)에 끼워 넣었는데, 이 방법론에서 세그먼트맵은 "시장·경쟁" 섹션(TAM-SAM-SOM 바로 뒤)에 있어야 하는 항목이었음 — 콘텐츠는 맞게 만들었지만 배치 위치를 놓침** | 사용자가 "마켓 세그먼트맵이 TAM-SAM-SOM 뒤에 있어야 되는데 왜 이상한 데 넣었어"라고 정확히 지적 | 위치 수정 필요 확인 — 파일이 PowerPoint에 열려 있어 이번 로그 작성 시점까지는 반영 대기 중 |
| 21 | 가치사슬 슬라이드 보강 콘텐츠 추천 | Claude Code가 이미 확보된 Five Forces MAU 데이터·KSF 목록·CJM 8단계·당근부동산 실측치를 가치사슬 단계에 재배치해 보강안 5가지(단계별 담당주체, 빈자리 도구 4종, 정량 체류기간, KSF 대비, 사업적 함의) 제안 | 새 데이터를 찾지 않고 이미 이 프로젝트에서 검증된 자료만 재조합해서, 근거 없는 내용을 지어내지 않고도 콘텐츠 밀도를 높이는 방법을 제시 | 특이사항 없음(아직 파일에 반영 전, 제안 단계) | 사용자가 반영 방식(텍스트로 직접 채울지, 참고자료로만 줄지)을 아직 정하는 중 | 보류 — 반영 방식이 정해지면 다음 단계에서 실행 |

---

## 종합 평가

- **AI가 특히 유효했던 영역**: 대량 문서(GitHub 11개 문서·PRD 47p 등)를 빠짐없이 읽고 교차 대조해 최신/구버전 충돌을 찾아내는 작업(#2, #4, #7, #9), 반복적인 코드 버그 수정(#5), 전수 검색 기반 일괄 치환(#12), 방법론 기준표와 실제 슬라이드를 1:1로 대조해 빠진 항목을 찾아내는 감사 작업(#17), 이미 확보된 데이터를 재조합해 근거 있는 콘텐츠 밀도를 높이는 작업(#21).
- **AI가 반복적으로 놓친 지점**: (1) **청중/독자 관점 고려 부족** — 원본 자료의 내부 코드·전문용어를 그대로 옮겨쓰는 경향(#9, #12), 심지어 같은 작업 중에 방금 없애기로 한 표현을 스스로 다시 써넣기도 함(#18). (2) **최신성 확인 습관 부족** — 질문에 바로 답하기 전에 가장 최신 1차 출처를 먼저 대조하지 않아 이후 정정이 필요했던 사례(#7). (3) **위임 범위 준수 실패** — 하위 작업 단위(서브에이전트)에 자율성을 넘겼을 때 범위를 벗어난 사례가 1건 발생(#10). (4) **분류·배치 판단 오류** — 내용 자체는 맞게 만들어도, 그것이 방법론상 어느 섹션에 속해야 하는지(시장·경쟁 vs 고객맥락)를 놓치는 사례가 발생(#20) — 콘텐츠 정확성과 구조적 위치 판단은 별개로 검증해야 함을 보여줌.
- **사람(팀)이 반드시 수행한 직접 판단**: 시장·세그먼트 전제 확정, 벤치마크 사례의 문제 적합성 판단, 전이·제외 판단, 최신 팀 결정 문서와의 정합성 확인, 발표 문서의 청중 적합성 판단, 산출물의 방법론상 배치 위치 확인. 이 항목들은 AI 산출물을 그대로 채택하지 않고 매번 원본 소스(GitHub 저장소, 실제 사용 경험, 스크린샷, 방법론 기준표)와 대조한 뒤에만 최종 반영했다.
